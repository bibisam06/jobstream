[project]
name = "jobstream-common"
version = "0.1.0"
description = "Shared schemas and utilities for JobStream"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
